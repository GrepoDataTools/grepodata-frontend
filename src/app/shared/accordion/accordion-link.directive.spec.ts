import { AccordionLinkDirective } from './accordion-link.directive';

describe('AccordionLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionLinkDirective();
    expect(directive).toBeTruthy();
  });
});
